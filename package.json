{
  "name": "omd-release-console",
  "version": "0.1.0",
  "private": true,
  "description": "OMD 双端发布控制台：Mac 统一发布，4090 只负责 Windows 构建。",
  "type": "module",
  "scripts": {
    "dev": "cargo run -- serve",
    "start": "cargo run -- serve",
    "check": "cargo fmt --check && cargo test && node --check public/app.js"
  }
}
