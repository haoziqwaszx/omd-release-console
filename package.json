{
  "name": "omd-release-console",
  "version": "0.1.0",
  "private": true,
  "description": "OMD 双端发布控制台：Mac 统一发布，4090 只负责 Windows 构建。",
  "type": "module",
  "scripts": {
    "dev": "cargo run -- desktop",
    "start": "cargo run -- desktop",
    "serve": "cargo run -- serve",
    "build": "cargo build --release",
    "check": "cargo fmt --check && cargo test && cargo clippy -- -D warnings && node --check public/app.js"
  }
}
