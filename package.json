{
  "name": "omd-release-console",
  "version": "0.1.0",
  "private": true,
  "description": "OMD 双端发布控制台：Mac 统一发布，4090 只负责 Windows 构建。",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check scripts/release-config.mjs && node --check scripts/release-state.mjs && node --check scripts/omd-release.mjs"
  }
}
